# Supabase setup for CivicChai

Place the SQL files in this folder and run them in the Supabase SQL editor in this order:

1. `schema.sql`
2. `policies.sql`
3. `seed.sql`

For migration-based workflows, use `migrations/001_init.sql` instead of the root schema file.
